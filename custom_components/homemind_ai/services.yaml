chat:
  name: Chat with HomeMind AI
  description: Send a message to HomeMind AI Assistant
  fields:
    message:
      name: Message
      description: The message to send to HomeMind AI
      required: true
      selector:
        text:
    user_id:
      name: User ID
      description: Optional user identifier
      required: false
      selector:
        text:
  target:
    entity:
